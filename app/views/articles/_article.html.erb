<article>
  <div>
    <div class="article_avatar">
      <%= image_tag avatar_url(article.user) if article.user %>
      <div><p><%= link_to(article.user.username, user_path(article.user)) if article.user %></p></div>
      <!--<div><p><%= article.user.username if article.user %></p></div>-->
    </div>

    <div class="article_title_date">
      <div class="article_title">
        <p>
          <%= article.title %>
        </p>
      </div>
    
      <div class="article_date">
        <i>Le <%= article.creation_date.to_date %></i>
      </div>
    </div>
  </div>

  <div class="article_content">
    <p>
      <%= article.content %> <%= link_to("Lire la suite...", article_path(article)) if read_more %></a>
    </p>
  </div>
</article>
